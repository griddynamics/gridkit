import { BASE_ANIMATION_MS, BASE_ANIMATION_DELAY_MS } from '@constants';

export const COMPONENT_NAME = 'Skeleton';

export const BASE_ANIMATION = `${BASE_ANIMATION_MS}ms ease-in-out ${BASE_ANIMATION_DELAY_MS}ms infinite`;
