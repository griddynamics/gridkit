export enum LinkVariant {
  Primary = 'primary',
  Secondary = 'secondary',
  Inverted = 'inverted',
  Inherit = 'inherit',
}

export enum LinkTarget {
  Blank = '_blank',
  Self = '_self',
  Parent = '_parent',
  Top = '_top',
  Framename = 'framename',
}

export enum LinkRel {
  Nofollow = 'nofollow',
  Noopener = 'noopener',
  Noreferrer = 'noreferrer',
}
