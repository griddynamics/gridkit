name: Evergreen Storybook CI/CD

on:
  workflow_dispatch:
  release:
    types:
      - published

permissions:
  contents: read
  actions: read
  checks: write
  id-token: write

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 'lts/*'
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test

  build:
    needs: test
    uses: ./.github/workflows/evergreen-build.yaml
    with:
      image-name: gd-gcp-rnd-evergreen/evergreen-v2/gridkit-storybook
      build-context: './'
      dockerfile: './Dockerfile.storybook'
  deploy:
    uses: ./.github/workflows/evergreen-deploy.yaml
    needs: build
    with:
      tenant: gridkit
      app-name: storybook
      env-name: dev
      image-tag: ${{ needs.build.outputs.image-tag }}
